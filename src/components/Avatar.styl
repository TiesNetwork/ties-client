color-secondary = rgba(0, 0, 0, .43)

.Avatar
  align-items: center
  background: color-secondary
  border-radius: 100%
  display: flex
  justify-content: center
  overflow: hidden

.AvatarContent
  max-height: 100%
  max-width: 100%
