.Rating {
  &Densed {
    align-items: center;
    display: flex;

    .RatingNumber {
      margin-left: 4px;
    }
  }
}

.RatingNumber {
  margin-top: 4px;
  text-align: right;
  white-space: nowrap;
}

.RatingStar {
  color: rgba(0, 0, 0, .1);

  &Active {
    color: #174DA1;
  }
}

.RatingStars {
  display: flex;
  font-size: 20px;
}
